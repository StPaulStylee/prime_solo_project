angular.module('pokerApp', ['ngRoute', 'btford.socket-io']);
      //  .factory('socket', function(){
      //   // I dont think URL is needed, but we'll include it just to be safe
      //   var socket = io.connect('http://localhost:3000');
      //   return socket;
